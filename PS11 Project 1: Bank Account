# print intro
# ask for username then password(check if they match in dictionary)
# if matched print home page input statement else try again
# if user picked 1, ask how much to deposit
# if user picked 2, ask how much to withdraw
# if user picked 3, ask for old password(check if old password in dictionary)
# if matched ask for new password else print password incorrect and go back to home screen
# if user picked 4, go back to log in page(step 2)

account = {"cashycat421":"Meowney124","bankbaddie375":"cashQueen573"}

balances = {"cashycat421":0,"bankbaddie375":0}

while True:
  while True:
    print("Welcome to the bank!Please log in.")
    username = input("Please enter your username and password username:")
    password = input("password:")
    if username in account:
      if password == account[username]:
        break
      else:
        print("Sorry,your username and password do not match")
    else:
      print("Sorry,your username and password do not match")
  while True:

    choice = input(
        "\nWelcome "
        + username
        + "! You currently have $"
        + str(balances[username])
        + " in your account. What would you like to do?\n\t1. Make a deposit\n\t2. Make a withdrawal\n\t3. Change your password\n\t4. Log out\n")
 
    if choice == "1":
        deposit = input("How much would you like to deposit? ")
        balances[username] += int(deposit)
    elif choice == "2":
        withdraw = input("How much would you like to withdraw? ")
        balances[username] -= int(withdraw)

    elif choice == "3":
        old_password = input(
            "To change your password, please type in your old password:")

        if old_password == accounts[username]:
            new_password = input("Please type in your new password:")
            accounts[username] = new_password

            print("\nYour password has been changed successfully!")
        else:
            print("\nYour old password does not match the password in our records.")
    elif choice == "4":
      break





